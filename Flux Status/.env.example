# This is an Application API key created in the Fluid panel. Never use a client key here.
PANEL_API_KEY=ptla_replace_me
# Compose routes this internally, so the status page does not need to call the public internet.
PANEL_URL=http://panel
