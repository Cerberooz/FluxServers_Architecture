# Runtime dependencies.
#
# Verified against `pip-audit` — see CI. Do not lower these pins without
# re-running the audit: the previous set (Flask 2.3.2 / Werkzeug 2.3.6 era)
# carried known CVEs, and so did the first replacement set.
Flask==3.1.3
Werkzeug==3.1.6
Jinja2==3.1.6
requests==2.33.0
urllib3==2.7.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7
alembic==1.13.3
Flask-WTF==1.2.1
WTForms==3.1.2
Flask-Limiter==3.8.0
# Flask-Limiter's Redis storage backend requires redis-py at runtime.
redis==5.2.1
python-dotenv==1.2.2
stripe==10.12.0
psycopg2-binary==2.9.10
cryptography==50.0.0
gunicorn==23.0.0
