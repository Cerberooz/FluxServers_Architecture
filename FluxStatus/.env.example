# This is an Application API key created in the Fluid panel. Never use a client key here.
PANEL_API_KEY=ptla_replace_me
# Same-VPS compose overrides this internally. On the public/status VPS, use the
# public Panel hostname because the Panel is on a different machine.
PANEL_URL=https://panel.fluxservers.cloud
